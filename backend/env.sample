# Sample environment variables for production / staging
PORT=8080

# Database: for production use a managed Postgres DSN
# Example URL form: postgres://user:pass@host:5432/dbname?sslmode=require
# For docker-compose local dev, you can use: host=db user=postgres password=postgres dbname=Civicissue port=5432 sslmode=disable
DATABASE_DSN=

# JWT secret (generate a strong random string)
JWT_SECRET=CHANGE_ME_SUPER_SECRET

# CORS: leave empty for same-origin (backend serves frontend). If using a separate frontend domain, set it.
# Example: https://your-frontend.example.com
ALLOWED_ORIGIN=

# Optional Redis (leave blank to disable)
REDIS_ADDR=
REDIS_PASSWORD=

# Disable auth ONLY in local dev (do not enable in prod)
DISABLE_AUTH=false
DEV_TEST_USER_EMAIL=dev@example.com
DEV_TEST_USER_PASSWORD=devpass

# ML APIs (leave blank to disable features gracefully)
ML_API_URL=https://urgency-api-latest.onrender.com/predict
IMAGE_CLASSIFICATION_API_URL=https://issue-classification-api.onrender.com/predict

# Frontend directory inside container/image
FRONTEND_DIR=/app/frontend
